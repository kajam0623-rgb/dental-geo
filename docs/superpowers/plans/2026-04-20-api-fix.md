# API 버그 수정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gemini/OpenAI API 호출 오류를 수정하여 분석 기능이 실제로 동작하도록 한다.

**Architecture:** analyze.ts의 queryGemini는 Google Search Grounding(`googleSearch`)을 사용하도록 수정하고, queryChatGPT는 OpenAI Responses API(`web_search_preview`)로 전환한다. API 라우트에 타임아웃 설정을 추가하고 모달 스크롤바 CSS를 완성한다.

**Tech Stack:** Next.js 16.2.3, @google/generative-ai (latest), openai ^4.0.0, Tailwind CSS v4

---

### Task 1: `.env.local` 생성

**Files:**
- Create: `.env.local`

- [ ] **Step 1: `.env.local` 파일 생성**

```
GEMINI_API_KEY=여기에_Gemini_API_키_입력
OPENAI_API_KEY=여기에_OpenAI_API_키_입력
```

- [ ] **Step 2: `.gitignore`에 `.env.local` 있는지 확인**

`.gitignore` 파일을 열어 `.env.local`이 포함되어 있는지 확인한다. (Next.js 기본 `.gitignore`에는 포함됨)

---

### Task 2: Gemini API 수정

**Files:**
- Modify: `src/utils/analyze.ts` (queryGemini 함수, 약 22-41번째 줄)

- [ ] **Step 1: `queryGemini` 함수 수정**

`src/utils/analyze.ts`의 `queryGemini` 함수를 다음과 같이 변경한다:

```typescript
async function queryGemini(prompt: string, clinicName: string): Promise<QueryResult> {
  if (!process.env.GEMINI_API_KEY) return { mentioned: false, responseText: '[API 키 없음]' };
  
  try {
    const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);
    const model = genAI.getGenerativeModel({
      model: "gemini-2.0-flash",
      tools: [{
        googleSearch: {}
      }],
    });
    
    const result = await model.generateContent(prompt);
    const text = result.response.text() || '';
    return { mentioned: isMentioned(text, clinicName), responseText: text.trim() };
  } catch (error) {
    console.error(`Gemini Error on prompt "${prompt}":`, error);
    return { mentioned: false, responseText: `[오류 발생] ${error instanceof Error ? error.message : String(error)}` };
  }
}
```

변경 포인트:
- `model: "gemini-1.5-flash"` → `model: "gemini-2.0-flash"`
- `googleSearchRetrieval: {}` → `googleSearch: {}`
- `// @ts-ignore` 제거

- [ ] **Step 2: node_modules에서 실제 타입 확인**

```bash
grep -r "googleSearch" node_modules/@google/generative-ai/dist/ --include="*.d.ts" | head -20
```

출력에 `googleSearch` 프로퍼티가 보이면 타입이 맞는 것. `@ts-ignore` 없이도 컴파일 가능.

---

### Task 3: OpenAI Responses API로 전환

**Files:**
- Modify: `src/utils/analyze.ts` (queryChatGPT 함수, 약 44-63번째 줄)

- [ ] **Step 1: `queryChatGPT` 함수를 Responses API로 교체**

`src/utils/analyze.ts`의 `queryChatGPT` 함수를 다음과 같이 전면 교체한다:

```typescript
async function queryChatGPT(prompt: string, clinicName: string): Promise<QueryResult> {
  if (!process.env.OPENAI_API_KEY) return { mentioned: false, responseText: '[API 키 없음]' };

  try {
    const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });
    const response = await openai.responses.create({
      model: 'gpt-4o-mini',
      tools: [{ type: 'web_search_preview' }],
      input: prompt,
    });
    
    const text = response.output_text || '';
    return { mentioned: isMentioned(text, clinicName), responseText: text.trim() };
  } catch (error) {
    console.error(`ChatGPT Error on prompt "${prompt}":`, error);
    return { mentioned: false, responseText: `[오류 발생] ${error instanceof Error ? error.message : String(error)}` };
  }
}
```

변경 포인트:
- `openai.chat.completions.create` → `openai.responses.create`
- `tools: [{ type: 'web_search' }]` → `tools: [{ type: 'web_search_preview' }]`
- `messages: [...]` → `input: prompt`
- `response.choices[0]?.message?.content` → `response.output_text`
- `// @ts-ignore` 제거

- [ ] **Step 2: OpenAI SDK 버전 확인**

```bash
node -e "const o = require('./node_modules/openai/package.json'); console.log(o.version)"
```

v4.87.0 이상이면 `openai.responses` API 사용 가능. 미만이면 다음 명령으로 업데이트:

```bash
npm install openai@latest
```

---

### Task 4: API 라우트 타임아웃 설정

**Files:**
- Modify: `src/app/api/search/route.ts`
- Modify: `src/app/api/deep-scan/route.ts`

- [ ] **Step 1: search route에 maxDuration 추가**

`src/app/api/search/route.ts` 상단에 추가:

```typescript
export const maxDuration = 60;
```

파일 전체 결과:
```typescript
import { NextResponse } from 'next/server';
import { runAnalysis } from '@/utils/analyze';

export const maxDuration = 60;

export async function POST(request: Request) {
  try {
    const body = await request.json();
    const { clinicName, region, treatment } = body;

    const result = await runAnalysis({ clinicName, region, treatment });

    return NextResponse.json({ success: true, data: result });
  } catch (error) {
    return NextResponse.json({ success: false, error: '분석 중 오류 발생' }, { status: 500 });
  }
}
```

- [ ] **Step 2: deep-scan route에 maxDuration 추가**

`src/app/api/deep-scan/route.ts` 상단에 추가:

```typescript
export const maxDuration = 60;
```

파일 전체 결과:
```typescript
import { NextResponse } from 'next/server';
import { runDeepAnalysis } from '@/utils/analyze';

export const maxDuration = 60;

export async function POST(request: Request) {
  try {
    const body = await request.json();
    const { clinicName, region, treatment, prompt, repeatCount } = body;

    if (!prompt || !repeatCount) {
      return NextResponse.json({ success: false, error: '프롬프트와 반복 횟수가 필요합니다.' }, { status: 400 });
    }

    const result = await runDeepAnalysis({
      clinicName,
      region,
      treatment,
      prompt,
      repeatCount: Math.min(repeatCount, 20),
    });

    return NextResponse.json({ success: true, data: result });
  } catch (error) {
    return NextResponse.json({ success: false, error: '정밀 분석 중 오류 발생' }, { status: 500 });
  }
}
```

---

### Task 5: `custom-scrollbar` CSS 추가

**Files:**
- Modify: `src/app/globals.css`

- [ ] **Step 1: globals.css에 스크롤바 스타일 추가**

`src/app/globals.css` 파일 끝에 추가:

```css
/* Custom scrollbar for modals */
.custom-scrollbar::-webkit-scrollbar {
  width: 6px;
}

.custom-scrollbar::-webkit-scrollbar-track {
  background: transparent;
}

.custom-scrollbar::-webkit-scrollbar-thumb {
  background: #334155;
  border-radius: 3px;
}

.custom-scrollbar::-webkit-scrollbar-thumb:hover {
  background: #475569;
}
```

---

### Task 6: 빌드 확인 및 실행 테스트

**Files:** 없음 (확인 단계)

- [ ] **Step 1: 의존성 설치**

```bash
cd C:/Users/닥터원츠/Downloads/dental-geo-main/dental-geo-main
npm install
```

- [ ] **Step 2: TypeScript 빌드 확인**

```bash
npm run build
```

에러 없이 빌드 성공해야 함.

- [ ] **Step 3: 개발 서버 실행 후 테스트**

```bash
npm run dev
```

브라우저에서 `http://localhost:4001` 접속 → 치과명/지역/진료명 입력 후 분석 실행 → 결과가 표시되면 성공.

- [ ] **Step 4: 오류 발생 시 콘솔 로그 확인**

분석 실패 시 Next.js dev 서버 터미널 로그에서 `Gemini Error` 또는 `ChatGPT Error` 메시지 확인.
